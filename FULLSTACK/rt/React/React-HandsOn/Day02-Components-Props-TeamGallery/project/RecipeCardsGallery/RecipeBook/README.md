# 🍝 Recipe Cards Gallery

A simple and elegant React mini project to showcase multiple **recipe cards** using **props** and **reusable components**.  
Each card displays the recipe name, time, difficulty, and image — and shows ingredients on hover.  

---

## 🚀 Features
- Reusable `RecipeCard` component  
- Dynamic rendering using `map()`  
- Pure CSS hover effect (no hooks used)  
- Clean card layout with subtle animations  
- Responsive flex-based gallery design  

---

## 🧠 Concepts Used
| Concept | Description |
|----------|--------------|
| **Props** | Data passed from parent (`App.jsx`) to child (`RecipeCard.jsx`) |
| **Reusable Component** | Same component used for multiple recipes |
| **Array Mapping** | Rendering multiple recipe cards dynamically |
| **Conditional CSS** | Ingredients list appears only on hover |
| **Component Structure** | Organized files (`App.jsx`, `RecipeCard.jsx`, `App.css`) |

---

## 📂 Folder Structure
