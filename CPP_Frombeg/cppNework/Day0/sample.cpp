#include<stdio.h>
int main()
{
    float a=1.5, b=1.55;
    printf(" %f %f",a,b);
    if(a=b)
    {
   printf("a and b are equal\n %f %f",a,b);
   }
    else
   {
		     printf("a and b are not equal\n");}
    return 0;
}
